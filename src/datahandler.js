(function dataHandler() {
  // ********

  window.jobApp = window.jobApp || {};




document.querySelector("alert").addEventListener("submit", )
//I don't know if this goes in here or in the other file.


//I know this needs a promise in front of the then. I'm unclear whether or not I
//need to actually use the word "fetch" because there was a part in the lecture
//code that said "this produces a fetch".
  .then(function update("index.html", {
    method: POST,
    display: "block",
    body: "Your application has been received!"
  });











// ;
  // ********
}());
